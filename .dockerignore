node_modules
.next
.git
.gitignore
.env*
!.env.example
!.env.dev.example
scripts/
TODO.md
*.md
!README.md
# Sibling projects / test harness - not part of the webmail image
examples/
integration/
e2e/
**/node_modules
# Unit tests never ship in the image, and excluding them keeps the context's
# type check from seeing imports into excluded dirs (e.g. lite-scripts.test.ts
# importing scripts/lite/*.mjs, which scripts/ above keeps out of the image).
**/__tests__
